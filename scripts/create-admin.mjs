import {PrismaClient} from "@prisma/client";import bcrypt from "bcryptjs";const prisma=new PrismaClient();const name=process.env.ADMIN_NAME?.trim(),email=process.env.ADMIN_EMAIL?.trim().toLowerCase(),password=process.env.ADMIN_PASSWORD;if(!name||!email||!password||password.length<12){console.error("Defina ADMIN_NAME, ADMIN_EMAIL e ADMIN_PASSWORD com pelo menos 12 caracteres.");process.exit(1)}const passwordHash=await bcrypt.hash(password,12);await prisma.staffUser.upsert({where:{email},update:{name,passwordHash,role:"ADMIN",active:true,technicianId:null},create:{name,email,passwordHash,role:"ADMIN"}});console.log("Administrador criado/atualizado:",email);await prisma.$disconnect();