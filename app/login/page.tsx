"use client";import {useState} from "react";export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function go(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})}),j=await r.json();if(!r.ok)return setMsg(j.error||"Falha no acesso");location.href=j.role==="TECHNICIAN"?"/tecnico":j.role==="ADMIN"?"/admin":"/central"}return <main><header><strong>CARIRI</strong><span> CENTRAL 24h</span></header><section className="login"><p className="eyebrow">ACESSO RESTRITO</p><h1>Entrar</h1><form onSubmit={go}><input type="email" placeholder="E-mail" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Senha" value={password} onChange={e=>setPassword(e.target.value)} required/><button>ENTRAR</button></form>{msg&&<div className="notice">{msg}</div>}</section></main>}