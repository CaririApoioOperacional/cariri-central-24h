"use client";import {usePathname,useRouter} from "next/navigation";export default function AppNavigation(){const r=useRouter(),p=usePathname();if(p==="/login")return null;const home=()=>r.push(p.startsWith("/tecnico")?"/tecnico":"/central");return <nav className="app-nav screen-only" aria-label="Navegação"><button className="nav-back" onClick={()=>r.back()} title="Voltar"><i>‹</i><span><b>VOLTAR</b><small>PÁGINA ANTERIOR</small></span></button><button className="nav-home" onClick={home} title="Tela inicial"><i>⌂</i><span><b>HOME</b><small>TELA INICIAL</small></span></button><button className="nav-next" onClick={()=>r.forward()} title="Próximo"><span><b>PRÓXIMO</b><small>PRÓXIMA PÁGINA</small></span><i>›</i></button></nav>}