"use client";import {usePathname,useRouter} from "next/navigation";export default function AppNavigation(){const r=useRouter(),p=usePathname();if(p==="/login")return null;return <nav className="app-nav screen-only" aria-label="Navegação"><button onClick={()=>r.back()} title="Voltar">‹<span>VOLTAR</span></button><button onClick={()=>r.forward()} title="Próximo">›<span>PRÓXIMO</span></button><button onClick={()=>r.push(p.startsWith("/tecnico")?"/tecnico":"/central")} title="Início">⌂<span>HOME</span></button></nav>}