"use client";import {FormEvent,useEffect,useState} from "react";
type C={id:string;name:string;active:boolean;sites:{id:string;name:string;address?:string}[];contacts:{id:string;phone:string;name?:string}[]};
export default function CustomerManager(){const [rows,setRows]=useState<C[]>([]);const [name,setName]=useState("");const load=()=>fetch("/api/customers").then(r=>r.json()).then(setRows);useEffect(()=>{void load()},[]);async function add(e:FormEvent){e.preventDefault();await fetch("/api/customers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name})});setName("");load()}return <><form onSubmit={add} className="form"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Nome / razão social"/><button>Cadastrar cliente</button></form><div className="list">{rows.map(c=><article key={c.id}><b>{c.name}</b><small>{c.contacts.length} WhatsApp(s) • {c.sites.length} estabelecimento(s)</small></article>)}</div></>}
