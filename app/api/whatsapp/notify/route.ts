import {NextResponse} from "next/server";import {prisma} from "../../../../lib/prisma";import {session} from "../../../../lib/auth";import {messageText,whatsappWebUrl} from "../../../../lib/whatsapp";export async function POST(req:Request){const s=await session();if(!s||!["ADMIN","CENTRAL","SUPERVISOR"].includes(s.role))return NextResponse.json({error:"Acesso negado"},{status:403});const b=await req.json(),e=await prisma.event.findUnique({where:{id:b.eventId},include:{site:{include:{contacts:true,customer:{include:{contacts:true}}}}}});if(!e)return NextResponse.json({error:"Evento não encontrado"},{status:404});const all=[...e.site.contacts,...e.site.customer.contacts],contacts=all.filter((x,i,a)=>x.authorized&&a.findIndex(y=>y.phone===x.phone)===i),message=b.text||messageText(e,String(b.kind||e.status)),links=contacts.map(c=>({phone:c.phone,name:c.name,url:whatsappWebUrl(c.phone,message)}));await prisma.audit.create({data:{eventId:e.id,action:"WHATSAPP_"+String(b.kind||e.status)+"_PREPARED",actor:s.name}});return NextResponse.json({protocol:e.protocol,message,links})}