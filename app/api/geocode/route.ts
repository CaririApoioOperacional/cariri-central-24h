import {NextResponse} from "next/server";import {session} from "../../../lib/auth";export async function GET(req:Request){const s=await session();if(!s||!["ADMIN","CENTRAL","SUPERVISOR"].includes(s.role))return NextResponse.json({error:"Acesso negado"},{status:403});const u=new URL(req.url),address=u.searchParams.get("address")?.trim(),cep=(u.searchParams.get("cep")||"").replace(/\D/g,"");if(!address&&!cep)return NextResponse.json({error:"Endereço ou CEP obrigatório"},{status:400});const queries=[address,cep.length===8?cep+" Brasil":null].filter(Boolean) as string[];for(const q of queries){try{const x=new URL("https://nominatim.openstreetmap.org/search");x.searchParams.set("format","jsonv2");x.searchParams.set("limit","1");x.searchParams.set("countrycodes","br");x.searchParams.set("q",q);const r=await fetch(x,{headers:{"User-Agent":"CARIRI-Central-24h/1.0","Accept-Language":"pt-BR,pt;q=0.9"},cache:"no-store"});if(r.ok){const j=await r.json();if(j?.[0])return NextResponse.json({latitude:Number(j[0].lat),longitude:Number(j[0].lon),displayName:j[0].display_name})}}catch{}}return NextResponse.json({error:"Coordenadas não encontradas para este endereço"},{status:404})}