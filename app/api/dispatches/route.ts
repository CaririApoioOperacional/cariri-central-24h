import {NextResponse} from "next/server";import {prisma} from "../../../lib/prisma";export async function GET(req:Request){const u=new URL(req.url),technicianId=u.searchParams.get("technicianId");return NextResponse.json(await prisma.dispatch.findMany({where:technicianId?{technicianId}:{},include:{technician:true,event:{include:{site:{include:{customer:true}}}}},orderBy:{createdAt:"desc"},take:100}))}export async function POST(req:Request){const b=await req.json();if(!b.eventId||!b.technicianId)return NextResponse.json({error:"Evento e técnico obrigatórios"},{status:400});const existing=await prisma.dispatch.findFirst({where:{eventId:b.eventId,status:{notIn:["COMPLETED","CANCELLED"]}}});if(existing)return NextResponse.json({error:"Evento já possui despacho ativo"},{status:409});const d=await prisma.dispatch.create({data:{eventId:b.eventId,technicianId:b.technicianId}});await Promise.all([prisma.event.update({where:{id:b.eventId},data:{status:"DISPATCHED"}}),prisma.technician.update({where:{id:b.technicianId},data:{available:false}})]);return NextResponse.json(d,{status:201})}