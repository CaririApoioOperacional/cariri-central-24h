import {NextResponse} from "next/server";import bcrypt from "bcryptjs";import {prisma} from "../../../../lib/prisma";import {createSession} from "../../../../lib/auth";export async function POST(req:Request){const b=await req.json();const u=await prisma.staffUser.findUnique({where:{email:String(b.email||"").toLowerCase()}});if(!u||!u.active||!await bcrypt.compare(String(b.password||""),u.passwordHash))return NextResponse.json({error:"Credenciais inválidas"},{status:401});await createSession({id:u.id,name:u.name,email:u.email,role:u.role});return NextResponse.json({name:u.name,role:u.role})}