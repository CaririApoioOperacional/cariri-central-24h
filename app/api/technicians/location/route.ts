import {NextResponse} from "next/server";import {prisma} from "../../../../lib/prisma";export async function POST(req:Request){const b=await req.json();if(!b.id||typeof b.latitude!=="number"||typeof b.longitude!=="number")return NextResponse.json({error:"Localização inválida"},{status:400});return NextResponse.json(await prisma.technician.update({where:{id:b.id},data:{latitude:b.latitude,longitude:b.longitude}}))}