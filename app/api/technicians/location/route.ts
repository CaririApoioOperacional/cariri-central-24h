import {NextResponse} from "next/server";import {prisma} from "../../../../lib/prisma";import {session} from "../../../../lib/auth";export async function POST(req:Request){const s=await session();if(!s||s.role!=="TECHNICIAN")return NextResponse.json({error:"Acesso negado"},{status:403});const u=await prisma.staffUser.findUnique({where:{id:s.id},select:{technicianId:true}});if(!u?.technicianId)return NextResponse.json({error:"Técnico não vinculado"},{status:409});const b=await req.json();if(typeof b.latitude!=="number"||typeof b.longitude!=="number")return NextResponse.json({error:"Localização inválida"},{status:400});return NextResponse.json(await prisma.technician.update({where:{id:u.technicianId},data:{latitude:b.latitude,longitude:b.longitude,locationUpdatedAt:new Date()}}))}