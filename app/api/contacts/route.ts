import {NextResponse} from "next/server";import {prisma} from "../../../lib/prisma";import {session} from "../../../lib/auth";const phone=(v:string)=>String(v||"").replace(/\D/g,"");export async function POST(req:Request){const s=await session();if(!s||s.role!=="ADMIN")return NextResponse.json({error:"Acesso negado"},{status:403});const b=await req.json(),p=phone(b.phone);if(!p||!b.customerId)return NextResponse.json({error:"WhatsApp e cliente obrigatórios"},{status:400});return NextResponse.json(await prisma.whatsAppContact.create({data:{phone:p,name:b.name||null,customerId:b.customerId,siteId:b.siteId||null,authorized:b.authorized!==false}}),{status:201})}