import {NextResponse} from "next/server";import {prisma} from "../../../lib/prisma";export async function POST(req:Request){const b=await req.json();if(!b.eventId||(!b.note&&!b.url))return NextResponse.json({error:"Informe evidência ou observação"},{status:400});const e=await prisma.evidence.create({data:{eventId:b.eventId,type:b.type||"NOTE",url:b.url||null,note:b.note||null,latitude:b.latitude,longitude:b.longitude}});await prisma.audit.create({data:{eventId:b.eventId,action:"EVIDENCE_ADDED",actor:b.actor||"Técnico Externo",latitude:b.latitude,longitude:b.longitude}});return NextResponse.json(e,{status:201})}