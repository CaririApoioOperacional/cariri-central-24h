import {readFile} from "fs/promises";import path from "path";import {NextResponse} from "next/server";export const runtime="nodejs";export async function GET(_:Request,{params}:{params:Promise<{name:string}>}){try{const {name}=await params;if(!/^[a-f0-9-]+\.(jpg|png|webp)$/i.test(name))return new NextResponse("Inválido",{status:400});const b=await readFile(path.join(process.cwd(),"uploads",name));const ext=name.split(".").pop();return new NextResponse(b,{headers:{"Content-Type":ext==="png"?"image/png":ext==="webp"?"image/webp":"image/jpeg","Cache-Control":"private, max-age=3600"}})}catch{return new NextResponse("Não encontrado",{status:404})}}