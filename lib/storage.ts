import {mkdir,writeFile,readFile} from "fs/promises";import path from "path";const root=process.env.EVIDENCE_STORAGE_PATH||path.join(process.cwd(),"data","evidence");export async function putEvidence(key:string,data:Buffer){await mkdir(root,{recursive:true});await writeFile(path.join(root,key),data)}export async function getEvidence(key:string){return readFile(path.join(root,key))}