const digits=(v:string)=>String(v||"").replace(/\D/g,"");export function whatsappWebUrl(phone:string,body:string){const p=digits(phone);return "https://web.whatsapp.com/send?phone="+encodeURIComponent(p)+"&text="+encodeURIComponent(body)}export function statusText(e:{protocol:string;status:string;result?:string|null;site:{name:string}}){return ["CARIRI MONITORAMENTO • CENTRAL 24h","Protocolo: "+e.protocol,"Estabelecimento: "+e.site.name,"Status: "+e.status,e.result?"Resultado: "+e.result:""].filter(Boolean).join("\n")}