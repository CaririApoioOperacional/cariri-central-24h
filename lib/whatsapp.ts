export async function sendWhatsAppText(to:string,body:string){const token=process.env.WHATSAPP_ACCESS_TOKEN;const id=process.env.WHATSAPP_PHONE_NUMBER_ID;if(!token||!id)return {sent:false,reason:"WhatsApp não configurado"};const version=process.env.WHATSAPP_GRAPH_VERSION||"v23.0";const r=await fetch("https://graph.facebook.com/"+version+"/"+id+"/messages",{method:"POST",headers:{Authorization:"Bearer "+token,"Content-Type":"application/json"},body:JSON.stringify({messaging_product:"whatsapp",to,type:"text",text:{body}})});return {sent:r.ok,status:r.status}}