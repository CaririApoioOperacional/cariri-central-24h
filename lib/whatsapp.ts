const digits=(v:string)=>String(v||"").replace(/\D/g,"");export function whatsappWebUrl(phone:string,body:string){const p=digits(phone);return "https://web.whatsapp.com/send?phone="+encodeURIComponent(p)+"&text="+encodeURIComponent(body)}const statusLabel:Record<string,string>={OPEN:"Aberto",DISPATCHED:"Técnico Externo acionado",EN_ROUTE:"Técnico Externo a caminho",ON_SITE:"Técnico Externo no local",COMPLETED:"Atendimento concluído",CANCELLED:"Atendimento cancelado"};export function statusText(e:{protocol:string;status:string;result?:string|null;site:{name:string}}){return ["CARIRI MONITORAMENTO • CENTRAL 24h","Protocolo: "+e.protocol,"Estabelecimento: "+e.site.name,"Situação: "+(statusLabel[e.status]||e.status),e.result?"Resultado: "+e.result:"","Mensagem preparada pela Central 24h."].filter(Boolean).join("\n")}